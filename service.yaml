apiVersion: v1
kind: Service
metadata:
  name: retail-demand-service
spec:
  type: NodePort
  selector:
    app: retail-demand-forecasting
  ports:
  - protocol: TCP
    port: 8888
    targetPort: 8888